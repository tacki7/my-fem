# Roll FEM Lab — 作業メモ

ブラウザ上で常時回り続ける実時間 FEM。**状態が時間とともに動く**ことが、このリポジトリの
テストを普通の単体テストと違うものにしている。以下は実際にやらかした失敗から書いた再発防止。

## 検証の基本

### 1. 非同期の完了は「時間」でなく「信号」で待つ

**3 回続けて同じ失敗をした。** μ 逆算の検証で固定待ち（700 ms → 28 s → 75 s）を使い、
毎回まだ計算中の中間値を読んで「発散した」「悪化した」と誤判定した。実際のコードは
正しく収束していた。

- ❌ `await sleep(75000)` — 反復回数が変われば足りなくなる
- ❌ 「トーストが消えたら完了」— 押した直後はまだ**出ていない**ので即座に抜ける
- ✅ **開始を確認してから終了を待つ**: 進行中トースト（見出しに `追い込み中`）が
  出るのを待つ → それが消えて結果トーストが出るのを待つ
- ✅ さらに良い: 反復回数など「実際に走った証拠」を返させ、0 なら失敗扱いにする

判定不能なときは**待ちすぎる方に倒す**。テストが遅いのは困らないが、嘘の測定値は困る。

### 2. テストハーネスを先に疑う

コードのバグだと 2 回誤診した。実際はハーネス側だった:

- **スタンド表の input は行優先**（全段の圧下率 → 全段の板厚 → …）。
  列優先と思い込み、荷重欄のつもりで μ を書き換えていた。
  → インデックス決め打ちの前に `.map(i => i.value)` で全部出して並びを確認する
- 上記の固定待ち

**結論を出す前に、答えの分かっているケースでハーネスを校正する。**

### 3. 逆解の往復検証は「別の運転点」でやる

μ 逆算の致命バグを最初に見逃した原因。μ を設定 → 収束 → その荷重を入れ戻す、で
+0.00% 一致したので正しいと判断した。だが実装は「現在の運転点で測った補正係数」を
使っており、**同じ運転点なら必ず一致する**（誤差が定義上打ち消える）。

現在の状態と**違う**入力を与えて初めてバグが出た（最大 +60%）。

> 逆解は、現在の解と一致する入力では検証にならない。必ず現在の状態から離れた
> 入力で試す。実運用も「実測値を打ち込んで押す」であって、収束させ直してからではない。

### 4. 入力を変えた直後の出力は「変更前の値」

アプリ側（`settleLoads`）でもテスト側でも踏んだ。μ を動かした直後はまだ荷重が
反応しておらず、プラントは**前の運転点で完全に静定して見える**。そこを読むと
「動かした向きが逆だった」と誤判定し、同じ向きにもう一度動かす。これで探索が
下限まで暴走した。

収束判定に必要なもの:
1. 不感時間（変更後しばらく測らない）
2. 連続する複数窓で静定
3. **内側ループ自身の収束フラグ**（`agcSettled` / `agcIdle`）。荷重が静止していても
   スクリューがまだ歩いていることがある（＝`内側ループ待ち`）

### 5. 許容誤差は感度から決める

このモデルは `d ln P / d ln μ ≈ 0.15`。つまり**荷重の 1% 誤差が μ の 7% 誤差**になる。
「荷重が 1% 合っていれば十分」は成り立たない。逆解を作るときは先に感度を測り、
そこから必要な収束精度を逆算する。

### 6. 推測せず中間量を全部出す

kf のバグ（入側ひずみ無視で 3 段とも同じ 740 MPa）は、仮説を立てる前に
h0 / h1 / 荷重 / kf / R' / ひずみ を段ごとにダンプしたら 1 回で見つかった。
遠回りに見えて速い。

### 7. 単調性が使えるなら最適化より区間法

μ→荷重は単調増加。区間を挟めば解は逃げられない。割線法・勾配法は速いが、
一度でも変な点に飛ぶと（μ<0.005 で噛み込みが破綻し単調性自体が壊れる領域がある）
戻ってこられない。実際に割線法版は答え 0.06 に対して μ=0.001 まで落ちた。

## ヘッドレス検証の道具

`screencapture` はユーザーの画面を撮るので使わない（別タブを撮って混乱した）。
CDP 経由のヘッドレス Chrome を使う。スクラッチパッドに以下がある:

| スクリプト | 用途 |
|---|---|
| `shot.mjs <url> <out> <wait> <w> <h>` | 全画面スクショ |
| `crop.mjs <out> <x> <y> <w> <h> <scale>` | 現在のページの矩形切り出し（CSS px） |
| `exec.mjs '<式>'` | ページ内で式を評価 |

起動:

```bash
npm run dev -- --port 5177 --strictPort &
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new --disable-gpu --enable-unsafe-swiftshader --hide-scrollbars \
  --remote-debugging-port=9222 --user-data-dir=<tmp>/chrome-profile \
  --window-size=1700,1050 about:blank &
```

**十分に待ってから測る。** 3 スタンドが落ち着くのに 20〜30 秒かかる。

## クエリパラメータ（測定用）

`?stands=3&agc=ratio|gauge|force&load=700&h1=1.2&field=temperature&mesh=insane&tension=rigid|simple|dist&tctl=1&tscale=0.05`
`&mode=reverse&loadmodel=slab&slab=orowan&flat=roberts&cmap=turbo`
— 起動時点で条件を作れるので、UI を操作して状態を作るより再現性が高い。`h1` は #1 の出側板厚目標 [mm]
（#2 以降は各段の圧下率で下る）。不正な値は黙って無視される — 効いたかは `__lab.stands()` で確かめる。
一覧と意味は README「計測用クエリパラメータ」。

**数値を比べる計測は `&fixeddt&stopafter=N` を付ける。** 既定のモデル時間は壁時計なので、同じ URL でも
CPU の混み具合で値が変わる（600 回解いて #3 の荷重が 1136.3 / 1138.1 tonf）。`fixeddt` で 1 回 1/60 s 固定、
`stopafter=N` で N 回解いたら一時停止 → `__lab.solves >= N && !__lab.running` を待って読む。これで読み込み直しても
`__lab.stands()` がビット単位で一致する（`stepMs` など処理時間は除く）。node の `tools/sim2d` とは刻みが同じでも
ビット一致はしない（V8 の版で `Math.exp` などの丸めが違う。README 参照）。

## Git 運用

**`main` に直接コミットしない。** 作業はブランチを切ってから。

```bash
git switch -c feat/<やること>      # 例: feat/agc-methods, fix/mu-inverse
# …作業…
npm run check && npm run build     # 通してからコミット（check は tsc も含む）
git add -A && git commit
git push -u origin HEAD
gh pr create --fill                # main へのマージは PR 経由
```

ブランチ名は `feat/` `fix/` `refactor/` `docs/` のいずれかで始める。

**push 前に必ず確認すること**（`gh-push` スキルの機密スキャンと同じ）:

- 個人の絶対パス（`/Users/<name>/…`）がコード・ログ・設定に紛れていないか
- 認証情報・トークン・`.env` の類
- `.claude/settings.local.json` はマシン固有（スクラッチパッドの絶対パスを含む）なので
  `.gitignore` 済み
- `pic/` は書籍のスキャン画像。**著作物なので push しない**（`.gitignore` 済み）。
  ローカルには残るので参照には使える

リポジトリは **public**（2026-09 に履歴を GitHub の noreply メールに書き換えた上で公開）。
コミットのメールはその noreply アドレス（ローカル git config 済み）— 個人のメールに戻さない。

## ビルド

```bash
npx tsc --noEmit     # 型だけ
npm run build        # tsc + vite
npm run check        # tsc → 2D を node 用にビルド → 2D の回帰チェック一式（直列、約 1 分。どれか FAIL で非 0）
```

`check` は `tools/check.mjs` が `tools/` 以下の `.mjs` から印の付いたものを集めて回す。チェックを足すときは
そのスクリプトの import の上に `// @check` と、要るビルドごとに `// @check-build sim2d`（`build-esm.mjs` の引数）を書くだけ
（`package.json` や一覧は触らない）。ビルドは `tools/build-esm.mjs`（node だけで動くので Linux でも同じ）。
`node_modules` が無いと `npx tsc` は TypeScript ではない別の `tsc` パッケージを取りに行くので、先に `npm ci`。

数値を変えたら **README / docs/validation.md の実測値も直す**。両方に「測定時の条件」を
明記する運用になっている。
