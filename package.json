{
  "name": "roll-fem-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Realtime 2D FEM lab for elastic roll / nip contact simulation",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "check": "node tools/check.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
